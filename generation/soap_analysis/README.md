# Note on soap_descriptors_sparse.npz

The cached SOAP descriptor archive `soap_descriptors_sparse.npz` (96 MB) is distributed via
Globus (see the repository README, section "Large files via Globus"). Download it into this
folder before running the cached SOAP analyses. The Slurm wrappers and
`soap_validation_summary.json` remain here.
