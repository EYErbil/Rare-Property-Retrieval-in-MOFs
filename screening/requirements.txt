# =============================================================================
# rare-property-retrieval / screening — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
#
# NOTE: moftransformer requires PyTorch and PyTorch Geometric to be installed
#       first. Follow the MOFTransformer install guide to get the correct
#       torch/PyG versions for your CUDA setup, then run this requirements file:
#         https://github.com/hspark1212/MOFTransformer
# =============================================================================

# --- Core ML / DL -----------------------------------------------------------
torch>=1.12.0
pytorch-lightning>=1.8.0
moftransformer                    # MOFTransformer model + datamodules

# --- Scientific computing ---------------------------------------------------
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# --- Machine learning -------------------------------------------------------
scikit-learn>=1.0.0
xgboost>=1.5.0
imbalanced-learn>=0.9.0           # SMOTE oversampling

# --- Visualization ----------------------------------------------------------
matplotlib>=3.5.0
seaborn>=0.11.0

# --- Dimensionality reduction -----------------------------------------------
umap-learn>=0.5.0                 # UMAP embeddings visualization

# --- Structural descriptors --------------------------------------------------
ase>=3.22.0                       # Atomic Simulation Environment (CIF reading for SOAP)
dscribe>=2.1.0                    # SOAP descriptors for diversity analysis

# --- Utilities ---------------------------------------------------------------
joblib>=1.1.0                     # Model serialization (sklearn)
tqdm>=4.60.0                      # Progress bars
